
{% extends 'back.html.twig' %}

{% block title %}Offre{% endblock %}

{% block body %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>NomOffre</th>
                <td>{{ offre.nomOffre }}</td>
            </tr>
            <tr>
                <th>DatepubOffre</th>
                <td>{{ offre.datepubOffre ? offre.datepubOffre|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offre_index') }}">back to list</a>

    <a href="{{ path('app_offre_edit', {'id': offre.id}) }}">edit</a>

    {{ include('offre/_delete_form.html.twig') }}
{% endblock %}
