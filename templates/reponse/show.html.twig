{% extends 'back.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ reponse.reponse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse_index') }}"><button type="button" class="btn btn-primary btn-sm">Retour</button>
</a>

    <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}"><button type="button" class="btn btn-primary btn-sm">Modifier</button>
</a>

    {{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
