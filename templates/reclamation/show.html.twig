{% extends 'front.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>NomReclamation</th>
                <td>{{ reclamation.nomReclamation }}</td>
            </tr>
            <tr>
                <th>PrenomReclamation</th>
                <td>{{ reclamation.prenomReclamation }}</td>
            </tr>
            <tr>
                <th>DestinationReclamation</th>
                <td>{{ reclamation.destinationReclamation }}</td>
            </tr>
            <tr>
                <th>DescriptionReclamation</th>
                <td>{{ reclamation.descriptionReclamation }}</td>
            </tr>
            <tr>
                <th>TypeReclamation</th>
                <td>{{ reclamation.typeReclamation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_index') }}">back to list</a>

    <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}">edit</a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
